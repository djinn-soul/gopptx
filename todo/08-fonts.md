# 08 - Fonts

Source: `TODO.md` plus `web_sid/docs.aspose.com/slides/python-net/{embedded-font,custom-font,fallback-font,render-presentation-with-fallback-font,powerpoint-fonts,font-replacement,font-selection-sequence,getting-warning-callbacks-for-fonts-substitution-in-aspose-slides}`.

## Supported

| Item | Docs | Status | Notes |
| --- | --- | --- | --- |
| Load custom fonts | `custom-font/` | Supported | Load from folders or memory is marked `[x]`. |
| Use custom font folders during render/export | `custom-font/` | Supported | Custom font folders during rendering/export are marked `[x]`. |
| Fallback fonts | `fallback-font/` | Supported | Create fallback fonts and fallback font collections are marked `[x]`. |
| Render with fallback fonts | `render-presentation-with-fallback-font/` | Supported | Fallback-aware rendering/export is marked `[x]`. |
| Embedded fonts | `embedded-font/` | Supported | Embed, get, remove, and compress embedded fonts are marked `[x]`. |

## Not Supported

| Item | Docs | Status | Notes |
| --- | --- | --- | --- |
| Public embedded-font workflows | `embedded-font/` | Not supported | First-class public API coverage is still `[ ]`. |
| Persist fallback rules in file | `fallback-font/` | Not supported | User-defined fallback rules persisted into a presentation file remain `[ ]`. |
| Font inventory / inspection APIs | `powerpoint-fonts/`, `font-selection-sequence/` | Not supported | Explicit deck font inventory APIs remain `[ ]`. |
| Better substitution docs | `getting-warning-callbacks-for-fonts-substitution-in-aspose-slides/`, `font-replacement/` | Not supported | Stronger documentation coverage remains `[ ]`. |
